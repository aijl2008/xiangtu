<!--component/recommendItem/recommendItem.wxml-->
<view class="recommend-item-contaienr">
  <view class="user-msg-container">
    <image class="header" src="{{recommend.avatar||'/images/user-64.png'}}"></image>

    <view class="detail-container">
      <text class="nick-name">{{recommend.nickname}}</text>
      <text class="msg">视频数{{recommend.uploaded_number}} · 粉丝{{recommend.be_followed_number}}</text>
    </view>

    <block wx:if="{{!recommend.followed}}">
      <view class="follow-btn" bindtap="followUser">+关注</view>
    </block>
  </view>

  <block wx:if="{{recommend.video.length}}">
    <view class="video-list-container">
      <block wx:for="{{[0,1,2]}}" :key="*this">
        <block wx:if="{{recommend.video[item]}}">
          <image class="video-item" src="{{recommend.video[item].cover_url||'/images/video_default_cover.png'}}"
                 bindtap="jumpVideoDetail" data-id="{{recommend.video[item].id}}"></image>
        </block>
        <block wx:else>
          <image class="video-item" src="/images/video_default_cover.png"></image>
        </block>

      </block>
    </view>
  </block>

</view>
